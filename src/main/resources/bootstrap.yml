spring:
  cloud:
    nacos:
      username:
      password:
      server-addr:
      discovery:
        namespace:
      config:
        namespace:
        timeout: 3000
        refresh-enabled: true  #修改后自动加载
        group:
        prefix:
        file-extension:
