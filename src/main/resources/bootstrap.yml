server:
  port: 11111

spring:
  application:
    name: openrum-collector
  cloud:
    nacos:
      username:
      password:
      discovery:
        server-addr: 10.241.60.20:8848
        namespace: RUM_ALI
        group: openrum-collector
      config:
        server-addr: 10.241.60.20:8848
        namespace: RUM_ALI
        timeout: 3000
        refresh-enabled: true  #修改后自动加载
        group: openrum-collector
        prefix:
        file-extension: yaml
